spring:
  application:
    name: service-b
  profiles:
    active: dev
  cloud:
    config:
      uri: http://config-server:8888
      username: configuser
      password: ${CONFIG_SERVER_PASSWORD:configpass123}
      fail-fast: true
      retry:
        max-attempts: 5
        initial-interval: 1000
        multiplier: 1.5
        max-interval: 5000
